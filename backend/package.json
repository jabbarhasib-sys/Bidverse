{
  "name": "bidverse-backend",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "predev": "node --input-type=module -e \"import{execSync}from 'child_process';try{execSync('npx kill-port 5001',{stdio:'ignore'})}catch(e){}\"",
    "dev": "nodemon --ignore database/data/ src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.1.0",
    "mongoose": "^8.4.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
